{
  "name": "@preproute/backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "npm run build && npm start"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.7.2"
  }
}